import 'package:flutter/material.dart';

class PantallaTarjetas extends StatelessWidget {
  const PantallaTarjetas({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Patalla tarjetas"),
      ),
      body: Placeholder(),
    );
  }
}